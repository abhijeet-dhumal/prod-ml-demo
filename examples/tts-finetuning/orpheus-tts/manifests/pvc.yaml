apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: orpheus-tts-storage
  namespace: smartshop
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: nfs-csi
  resources:
    requests:
      storage: 150Gi
